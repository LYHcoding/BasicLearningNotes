# 启用打破平局

作用: 1. 当进行多分类任务时可以启用  2. 启用后，多分类策略会被设置为 ovr  3. 作用在于，当两个类别具有相同的分类概率时，则根据类名排序，返回第一个类，如果启用之后，则会根据decision_function的置信度来决定返回的类  4. 启用之后，会增加计算开销，降低拟合好后的模型预测效率
参数: break_ties
数据: 1. bool类型，默认值为Flase